The Python application Requires Python 3.0 or better, pip, Anaconda for python (for NLTK, Scikit-learn, Matplotlib).

(It's recommended to keep the directory structure as submitted, for successfull execution of pyhton program.)

>> Run the Tokenize_WebServices.py.

>> Some files will be generated.

>> Now run the clustering.py program.

>> Cluster nfomation and Visalisation will be generated.